<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="snapshot.behavior.category.mapper.CategoryMapper">

    <select id="selectCategoryList" parameterType="snapshot.behavior.category.dto.CategoryDTO" resultType="snapshot.behavior.category.dto.CategoryDTO">
        select * from behavior_category
    </select>

    

</mapper>
